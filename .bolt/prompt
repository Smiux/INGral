# 项目开发规则

## 1. 代码质量
- 使用 `npm run typecheck` 和 `npm run lint` 检查代码质量，使用 `npm run check` 同时检查两个工具的结果
- 代码提交前必须通过 typecheck 和 lint 检查
- 使用 `npm run lint:fix` 自动修复 lint 问题,或使用 `npm run check:fix` 自动修复 typecheck 和 lint 问题
- 使用 `npm run check` 检查代码质量，同时检查 typecheck 和 lint 结果
- 不运行 `npm run build`,使用 `npm run check` 检查代码质量即可

## 2. 代码风格
- 遵循 TypeScript 严格模式，所有变量和函数必须有明确的类型定义
- 深入了解当前的 ESLint 规范要求，在开发过程中主动遵守，避免出现需要修复的问题
- 代码不考虑使用 aria 属性，不要添加 aria 属性
- 使用路径别名 `@/` 代替相对路径导入
- 不要添加无用注释，代码变动不注释代码变动，添加注释时只描述当前代码
- 注释必须中文，不能使用英文
- 禁止使用console.log 等调试语句

## 3. 性能优化
- 状态管理保持精简，避免过度复杂化
- 避免在渲染过程中创建新函数或对象

## 4. UI 设计规范
- 遵循设计系统规范，统一使用项目中定义的组件库和样式系统
- 使用tailwindcss中定义的颜色变量，避免硬编码颜色值，参考此文档E:\project\docs\TAILWIND_COLORS.md
- 确保颜色对比度符合 WCAG AA 级以上标准
- 使用统一的字体大小、行高和字重
- 确保在不同屏幕尺寸下都能正常显示
- 使用 Lucide React 图标库

## 5. 项目特有规范
- 项目不使用用户系统及相关概念功能
- 所有设计和开发不考虑用户认证、授权、权限管理等相关功能
- 不存储和处理用户个人信息
- 所有数据处理必须考虑匿名性