// Re-export theme context and types for backward compatibility
export { ThemeContext } from './ThemeContext';
export type { Theme, ThemeContextType } from './ThemeContext';
export { useTheme } from '../hooks/useTheme';
